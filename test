const promesseDeCafé = new Promise((resolve, reject) => {
    let caféDisponible = true;

    setTimeout(() => {
        if (caféDisponible) resolve("☕ Café prêt !");
        else reject("😞 Pas de café aujourd’hui");
    }, 2000);
});

promesseDeCafé
    .then((message) => console.log(message))
    .catch((erreur) => console.log(erreur));
